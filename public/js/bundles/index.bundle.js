document.addEventListener('DOMContentLoaded',()=>{const hamburger = document.getElementById('hamburger');const nav = document.getElementById('main-nav');if(hamburger && nav){hamburger.addEventListener('click',()=>{nav.classList.toggle('active');});}const swiperEl = document.querySelector('.swiper');if(swiperEl && typeof Swiper !== 'undefined'){new Swiper(swiperEl,{loop:true,autoplay:{delay:4000,disableOnInteraction:false,},effect:'fade',fadeEffect:{crossFade:true,},});}const swiperLateralEl = document.querySelector('.swiperlateral');if(swiperLateralEl && typeof Swiper !== 'undefined'){new Swiper(swiperLateralEl,{loop:true,autoplay:{delay:5000},pagination:{el:'.swiper-pagination',clickable:true},});}});document.addEventListener("DOMContentLoaded",async function(){const container = document.getElementById("proximas-partidas");if(!container)return;const urls = [ "torneos/1v1_copa_uruguaya_2026.json",{name:"A",url:"https:{name:"B",url:"https:{name:"C",url:"https:{name:"D",url:"https:{name:"E",url:"https:];function cleanNick(nick){return(nick || '').replace(/[|\[\]]/g,'').trim();}function parsearFechaCopa(fecha,horario){if(!fecha || !horario)return null;try{const [d,m,y] = fecha.split('-');const [hh,mm] = horario.split(':');return new Date(y,m - 1,d,hh,mm);}catch(e){return null;}}function parsearFechaLiga(p){if(!p.fecha || !p.horario)return null;try{const [d,m,a] = p.fecha.split(/[-/]/);const horaLimpia = p.horario.replace('.',':');const [hh,mm] = horaLimpia.includes(':')? horaLimpia.split(':'):[horaLimpia,'00'];return new Date(a,m - 1,d,hh,mm);}catch(e){return null;}}function renderDueloCopa(j1,j2,grupo,fechaObj,horario,diaSemana,coordinadoPor){const item = document.createElement("div");item.className = "match-item";let fechaStr = '';if(fechaObj && horario){fechaStr = `${diaSemana}${fechaObj.getDate()}de ${fechaObj.toLocaleString('es-UY',{month:'short'})}· ${horario}`;}item.innerHTML = ` <div class="match-header"> <span class="match-badge">${grupo}</span> ${coordinadoPor ? `<span class="match-coord">Coord:${coordinadoPor}</span>`:''}</div> <div class="match-players"> <span class="player">${cleanNick(j1)}</span> <span class="vs">vs</span> <span class="player">${cleanNick(j2)}</span> </div> ${fechaStr ? ` <div class="match-date"> <i class="far fa-calendar-alt"></i> ${fechaStr}</div>`:''}`;return item;}function renderDueloLiga(j1Nombre,j2Nombre,cat,fecha){const item = document.createElement("div");item.className = "match-item";item.innerHTML = ` <div class="match-header"> <span class="match-badge">Cat ${cat}</span> </div> <div class="match-players"> <span class="player">${j1Nombre}</span> <span class="vs">vs</span> <span class="player">${j2Nombre}</span> </div> <div class="match-date"> <i class="far fa-clock"></i> ${fecha.toLocaleString('es-UY',{weekday:'short',day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'})}</div> `;return item;}async function processCopa(){try{const res = await fetch(urls[0]);if(!res.ok){console.warn('No se pudo cargar Copa Uruguaya:',res.status);return null;}const data = await res.json();if(!data.grupos || !data.rondas_grupos)return null;const todosLosPartidos = [];data.rondas_grupos.forEach(rg =>{rg.partidos.forEach(ronda =>{ronda.partidos.forEach(partido =>{if(partido.resultado === null && partido.fecha && partido.horario){const fechaObj = parsearFechaCopa(partido.fecha,partido.horario);if(fechaObj){todosLosPartidos.push({j1:partido.jugador1Nick,j2:partido.jugador2Nick,grupo:`Grupo ${rg.grupo}`,fecha:fechaObj,horario:partido.horario,diaSemana:partido.diaSemana || '',coordinadoPor:partido.coordinadoPor || ''});}}});});});todosLosPartidos.sort((a,b)=> a.fecha - b.fecha);const proximos = todosLosPartidos.slice(0,2);console.log('Partidos coordinados encontrados:',todosLosPartidos.length);if(proximos.length === 0)return null;return{type:'copa',matches:proximos};}catch(e){console.error('Error procesando Copa:',e);return null;}}async function processLiga(){try{const ligaUrls = urls.slice(1);const resultados = await Promise.all(ligaUrls.map(cat => fetch(cat.url).then(res => res.json()).catch(()=> null)));const ahora = new Date();let todosLosDuelos = [];resultados.forEach((data,index)=>{if(!data || !data.jornadas)return;const participantes = data.participantes || [];data.jornadas.forEach(j =>{(j.partidos || []).forEach(partido =>{const fechaReal = parsearFechaLiga(partido);if(fechaReal && fechaReal > ahora){todosLosDuelos.push({cat:ligaUrls[index].name,p:partido,fecha:fechaReal,parts:participantes});}});});});todosLosDuelos.sort((a,b)=> a.fecha - b.fecha);const proximos = todosLosDuelos.slice(0,2);if(proximos.length === 0)return null;return{type:'liga',matches:proximos};}catch(e){console.error('Error procesando Liga:',e);return null;}}try{const copaResult = await processCopa();if(copaResult && copaResult.type === 'copa' && copaResult.matches.length > 0){container.innerHTML = '';copaResult.matches.forEach(match =>{container.appendChild(renderDueloCopa(match.j1,match.j2,match.grupo,match.fecha,match.horario,match.diaSemana,match.coordinadoPor));});}else{const ligaResult = await processLiga();if(ligaResult && ligaResult.type === 'liga' && ligaResult.matches.length > 0){container.innerHTML = '';ligaResult.matches.forEach(duelo =>{const j1 = duelo.parts.find(p => p.id === duelo.p.jugador1Id)?.nombre || "A definir";const j2 = duelo.parts.find(p => p.id === duelo.p.jugador2Id)?.nombre || "A definir";container.appendChild(renderDueloLiga(j1,j2,ligaResult.matches.cat,duelo.fecha));});}else{container.innerHTML = `<p class="empty-msg">No hay partidas coordinadas por ahora.</p>`;}}}catch(error){console.error('Error general:',error);container.innerHTML = `<p class="empty-msg">Error al sincronizar.</p>`;}});document.addEventListener("DOMContentLoaded",async()=>{const container = document.getElementById("torneos-globales-content");try{const res = await fetch('/api/torneos');if(!res.ok)throw new Error("No se pudo obtener la lista de torneos");const torneos = await res.json();if(!torneos || torneos.length === 0){container.innerHTML = "<p>No hay torneos anunciados por ahora.</p>";return;}const ahora = new Date();const torneosFuturos = torneos.filter(torneo => new Date(torneo.start)> ahora);if(torneosFuturos.length === 0){container.innerHTML = "<p>No hay torneos futuros anunciados.</p>";return;}const getTierIcon =(tier)=>{if(!tier)return "❓";if(tier.includes("S-Tier"))return "🟨";if(tier.includes("A-Tier"))return "🟩";if(tier.includes("B-Tier"))return "🟦";if(tier.includes("C-Tier"))return "🟪";return "❓";};torneosFuturos.sort((a,b)=> new Date(a.start)- new Date(b.start));const lista = document.createElement("ul");torneosFuturos.slice(0,5).forEach(torneo =>{const startDate = new Date(torneo.start).toLocaleDateString();const icono = getTierIcon(torneo.tier);const item = document.createElement("li");item.innerHTML = `${icono}<strong>${torneo.name}</strong> - ${startDate}`;lista.appendChild(item);});container.innerHTML = "";container.appendChild(lista);}catch(error){console.error("Error al cargar torneos globales:",error);container.innerHTML = "<p>No se pudieron cargar los torneos.</p>";}});